global:
  scrape_interval: 15s # Как часто собирать данные (каждые 15 секунд)

scrape_configs:
  - job_name: 'node_metrics'
    static_configs:
      - targets: ['node-exporter:9100'] # Обращаемся к датчику по имени его контейнера
